<Window x:Class="CalanderTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalanderTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button" x:Key="ButtonStyle">
            <Setter Property="Background" Value="LightBlue"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>
    <Window.DataContext>
    <local:ViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Content="Calender" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="30"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Calendar VerticalAlignment="Center" SelectedDate="{Binding SelectedDate}"/>
            <Button Grid.Column="1" Height="30" Width="200" Visibility="{Binding ButtonVisibility}" Content="Enter Date" Command="{Binding EnterComm}"/>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Visibility="{Binding PanelVisibility}" Margin="20">
                <Label Content="{Binding SelectedDate}"/>
                <StackPanel Orientation="Horizontal">
                    <ListBox ItemsSource="{Binding Photos}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <DockPanel/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Image Source="{Binding}" Width="150" Height="150" Margin="5"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Button Content="Upload Photo" Command="{Binding AddPhotoComm}"/>
                </StackPanel>
                <ListBox IsEnabled="False" ItemsSource="{Binding DateText}"/>
                <Label/>
                <TextBox Text="{Binding DateTextIn}"/>
                <Label/>
                <Button Content="Exit Date" Command="{Binding LeaveComm}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
